# Google Cloud Project ID
GOOGLE_PROJECT_ID=your-project-id-here

# Service Account JSON Key (Paste the entire JSON content on one line if possible, or handle multiline)
# For Vercel, simpler to store as one string or base64. 
# Here we expect the raw JSON string.
GOOGLE_APPLICATION_CREDENTIALS_JSON='{ "type": "service_account", ... }'
